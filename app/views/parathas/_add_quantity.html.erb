<%= form_with url: add_to_cart_path(id), method: :get, local: false do |form| %>
  <%= form.label :quantity, "Quantity:", class: "label" %>
  <%= form.text_field :quantity, class: "form-control" %>
  <%= form.submit "Add to Cart", class: "form_button" %>
<% end %>
